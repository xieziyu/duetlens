# 更新日志

本文件只记**对使用者可见**的变化。内部重构、文档与 CI 调整不单列,查 `git log`。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/);`0.x` 阶段 minor 位即破坏性变更位。

## [0.9.0] - 2026-08-25

### 新增

- **`cmd+w` 关掉当前的审核标签页。** 此前只能用鼠标关(x 按钮或中键),因为这个键归 Electron 默认的「关闭窗口」所有 —— 菜单快捷键在渲染层看到 keydown 之前就被处理掉,前端怎么拦都拿不回来。现在关窗口挪到 `shift+cmd+w`。关标签页复用原来那条路径,「还在后台跑」的提示与未发送草稿的确认都还在;标签条不在屏上时(入口、历史、设置)这个键仍然是关窗口 —— 没有标签页的时候它本来就是这个意思;有弹窗开着时它什么都不做,和 `ctrl-tab` 一样让位。(#84)
- **第一轮扫描就带上 PR 的标题、正文与评论。** PR 上下文此前只喂给重跑,第一轮 agent 手上只有一份 diff,于是内置的 Scope 检查(「正文承诺了 diff 里没有的东西」)在第一轮无从比对,要等到第二轮才开始起作用。现在第一轮就拿到标题、正文、PR 级评论与评审结论。散落的行内讨论仍留到第一次重跑再进:此刻还没有我们自己的 finding 供它挂靠,而它是整块上下文里最占体积的一部分;不需要它的那次抓取现在也跳过了查询里最贵的那段。超过 6000 字符的 PR 正文改为保留首尾、并注明中间省掉了多少 —— 勾选清单一律在正文末尾,而那正是 Scope 检查要读的东西。(#85)

### 升级须知

- 数据库结构不变,仍是 v22,与 0.8.0 之间可以来回安装。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.8.0] - 2026-08-24

### 新增

- **对话里 agent 的回复边生成边出现,整段等待也画在答案会落地的那一格。** 实测一次跟进的中位耗时是 12.6 秒(p90 33 秒),而最终文本只占最后两三秒 —— 前面那段一直只有三个跳动的点,尽管后端早就知道 agent 正在读哪个文件。现在气泡在原地经历排队、取证、回复、已中断四个阶段,取证行写明读了什么、找了什么、花了多久,文本落定时位置不跳。跟进过程中的取证也不再被并进扫描日志(此前会把「N/M 改动文件已读」的覆盖数虚高)。回复可以中断:已经出来的半句留在屏幕上并可复制,但不入库 —— 存下来的话下一轮会把半句话当成上文复述一遍;它是渲染态,切走 review 或清空对话就没了,卡片上写明了这点。滚动只在你本来就贴着底部时才跟,离开后给一颗跳回底部的按钮。已知限制:还在排队、尚未开始的跟进没有可中断的对象,停止按钮要等它真的开跑才出现。(#74)
- **多个审核可以同时开成标签页,最多 20 个。** 标签页只是视图把手,关掉一个从不碰它背后的 codex 会话;关一个还在扫描的会说明这件事并给一条回去的路。后台标签页保持挂载:在途的回复文本、本轮活动日志、没发出去的草稿和滚动位置都还在,切回来就是原样。上次开着的那些会随应用一起恢复(库里已经不存在的 id 会被丢掉,免得出现一个永远加载不完的空标签页)。ctrl-tab 逐个切换;在后台报上来的新 finding 有未读计数,切过去即清零;关掉一个还留着未发送文字的标签页会先问一句。提交/导出从「旁边的一屏」改成了标签页内部的一个视图 —— 作为并排的屏,它在切页之后回答不了「这是哪个审核的」。标签条放不下时会横向滚动并始终把当前页留在视野里(缩窄窗口、关掉相邻页、上限提示出现都算),两侧边缘渐隐提示还有更多,竖向滚轮在这里转成横向滚动。(#78, #80)
- **agent 正文里的标题和有序列表会正常渲染了。** 此前 `###` 与 `1.` 原样显示,而且这些行会被并进同一段,读起来是一长串没有分节的文字。标题不引入第二种正文字号,只用字重、颜色和间距分节,一二级加一条细分隔线。同时修掉一个从 finding 卡片支持 markdown 起就存在的死循环:带 `c++`、`objective-c` 这类信息串的代码围栏两个分支都不匹配,渲染器原地打转直到内存耗尽。(#75)

### 修复

- **上次退出时还在扫描的审核不会永远转圈了。** codex 会话随进程一起死掉,库里的行却不会:这条审核会一直停在「扫描中」。现在标签页会跨重启恢复,这就变成下次启动时一个永远停不下来的 spinner。冷启动会结算上一个进程留在半路的所有形态 —— 真的被打断的一轮、只写了一半的收尾、以及压根没有轮次行的审核。哪一侧已经落地就用哪一侧修另一侧:父状态已完成、轮次还停在扫描中的,判为完成而不是失败 —— 把一次跑完的扫描标成中断,等于凭空发一张失败卡片和一颗重试按钮。另外加了单实例锁(在打开数据库之前拿):没有它,第二个实例会把第一个正在跑的轮次判失败。第二次启动现在会聚焦已有窗口,窗口关光了就再开一个。(#77)
- **codex 的资源探测不再把整轮评审判失败。** codex 0.149 起,只要配了 MCP 服务器就会内置 `list_mcp_resources` 工具,并在描述里建议模型优先查资源;模型照做时会向 Duetlens 发 `resources/list`,而我们只声明了 tools 能力,SDK 回 -32601。codex 把这次失败记成 `server: "duetlens"` 名下的一次 MCP 工具调用,而「工具调用没送达」的兜底只按服务器名匹配,于是整轮扫描被杀掉、会话被拆。它取决于模型这一轮想不想探测,所以是间歇性的,报出来的还是一句指向 codex 版本不匹配的误导文案。现在服务器声明 resources 能力并对两个资源列表都回空表(我们确实不发布资源,空表是诚实答案),兜底也改成必须同时是我们声明过的工具名 —— 以后新增的 codex 内置工具自动同享这条豁免。(#76)
- **标签页不再把同一件事写两遍。** 后端把审核标题拼成 `<身份> · <主题>`,而标签页已经在源图标旁边画了身份,于是一个 PR 显示成 `#4218` + `#4218 · refactor(...)`,GitButler 分支显示成 `feat/x` + `GitButler · feat/x`,两半还都被截断。现在标题里重复的前缀会被剥掉(审核顶栏同样如此),标题可以一直缩到零宽再轮到身份被截;原生 `title` 提示在标签条上从来没弹出来过,换成了应用内的悬浮卡,一行项目加完整身份,一行完整标题。顺带修掉一处旧问题:填完整 PR URL 时,仓库名解析出来的是 owner。(#79)

### 升级须知

- 数据库结构升级到 v22(0.7.2 是 v21),旧库首次启动自动迁移,无需操作。**迁移是单向的** —— 装回 0.7.x 仍能打开迁移过的库,只是新增列上的内容(上次开着的审核标签页与当前活跃项)它看不见,也不会写回去。真要降级,先备份 `~/Library/Application Support/Duetlens/duetlens.db`。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.7.2] - 2026-08-24

### 修复

- **codex 0.149 起,评审不再静默地报告「0 个问题」。** 这一版把 MCP 工具调用也纳入了审批,`approvalPolicy: "never"` 的含义从「不问、放行」翻成了「不问、拒绝」,于是 agent 每一次 `report_finding` / `write_summary` 都被挡下,一轮评审跑到底、结论页干干净净、日志里也没有任何异常 —— 与「审完确实没发现问题」在界面上完全一样。现在握手时改为申请 `granular` 审批策略并把五道闸门全部关死(这是当前唯一表达「不问且放行」的写法),codex 若不认这个策略再退回 `"never"`;老版本 codex 会忽略它读不懂的字段,因此不需要第二次握手。另外,codex 若拒绝把工具调用交给内置 MCP 服务器,这一轮会直接判失败并报错,不会再退化成一份空结论。(#72)

### 升级须知

- 数据库结构不变,仍是 v21,与 0.7.1、0.7.0 之间可以来回安装。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.7.1] - 2026-08-21

### 修复

- **从 Dock / Spotlight 启动时,codex 需要的环境变量不再丢一半。** launchd 给的环境近乎是空的,此前只补回了 `PATH`,其余变量一律丢掉:codex 配置里带 `env_key` 的自定义 `model_providers`、裸的 `OPENAI_API_KEY`、代理变量,在图形启动下全都读不到,评审一开跑就死在「missing environment variable X」;而同一份配置在终端里跑 codex CLI 一切正常,这个差异几乎无法自查。现在同一次登录 shell 探测会把整份环境带回来,`PATH` 保持原有的合并与兜底目录逻辑,其余变量只在当前进程没有该键时补上 —— launchd 或命令行传进来的仍然优先。会改变本进程行为的开关(`ELECTRON_RUN_AS_NODE`、`NODE_OPTIONS` 等)与本应用自己的启动开关(`DUETLENS_USER_DATA`)不会被采纳。探测结果只留在内存里:不写日志、不落库、不过 IPC。(#70)

### 升级须知

- 数据库结构不变,仍是 v21,与 0.7.0 之间可以来回安装。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.7.0] - 2026-08-20

### 新增

- **可以选审核的对比基线了。** 之前只能选「审哪个分支」,拿它跟什么比是后端自动探测默认分支定下的,所以一条 stack 上的分支只能一层一层单独看,想连着下面几层一起审做不到。现在入口页的分支选择器旁边多一个基线选择器,每一行写明它覆盖的范围,配 stack 阶梯与实时的改动面计数;GitButler 源给出同一条 stack 里更低的那几个分支加 workspace 基座,本地仓库源放宽到全部本地分支。选定的基线落库,重跑与恢复沿用同一条,不会第二轮起悄悄换一条基线。(#58)
- **github-pr 源可以把一个 stacked PR 连着它下面的 PR 一起审。** `gh pr diff` 只给一个 PR 自己那一段 diff,叠在上面的 PR 单独看等于没有下层上下文。现在会顺着 `baseRefName` 往下找出整条祖先链(以某个分支为 head 的那个 PR 就是它下面一层),再从 compare API 取加宽后的 diff,不需要 clone。提交前的校验仍按 PR 自己的 diff 走 —— GitHub 认的就是这一份 —— 锚点落在下层 PR 上的 finding 会说明「不在本 PR 范围内」,而不是含糊地说「过期」。(#59)

### 修复

- **GitButler 工作区里审一条分支,不再读到别的分支的改动。** diff 里只有该分支的提交,而 `get_file` 与 `search_code` 读的是工作区 —— GitButler 的工作区是所有已应用分支合并后的样子,于是 agent 会引用一段本次评审从未展示过的代码,界面上没有任何提示;跟随仓库里一个指向外部的符号链接还能读到仓库之外。现在准备阶段就把分支头钉住,取证一律从那棵树读,准备之前的取证调用直接拒绝(否则读到的是索引)。diff 只取一次并要求它出自被钉住的那个头:取证开始前分支动过会重新钉一次,取证之后再动则报错,而不是悄悄换一棵树读下去。顺带修掉两处会静默换基线的:短名解析改为先 `refs/heads` 再 `refs/remotes` 并 peel 到 commit(同名 tag 会赢下短名查找);同名 tag 存在时拒绝走默认 diff(`but diff` 只认名字,在那里返回的是空改动集)。另外改动面数不出来时显示「未知」而不是「0 个文件」,冲突中的分支不再看起来像没改动。(#61)
- **入口页的等待状态看得见了,基线区块也不再跳。** 这一屏有六处在途状态,各画各的,清一色是不动的浅灰小字,现在统一成一个 Busy 指示(agent 配色的转环加一行文字)。祖先链探测从 PR 卡片的元数据行挪到基线选择器自己那一格 —— 几秒钟的等待藏在密排元数据的说明文字里没人看得见;占位行与真实行等高,换成选择器时页面不跳,探测下来发现不是 stacked PR 则带过渡收起,而不是直接抽掉一行让页面跳 50px。还有几处会让人审错东西的:探测只按 PR 身份做 key(它自带 owner/repo,仓库路径那条回退从来没生效过),此前改一下路径就重探,候选列表空白好几秒,选择器与范围警告一起消失而已选的基线仍然生效;首层链探测失败现在报错而不是返回空链(有效的 PR 一定有 base,空链只可能是探测本身失败,吞掉它会把限流伪装成「不是 stacked」);链稳定后若已选基线不在候选里就清掉它,免得在一条比屏幕上显示的更宽的基线上开审。(#66)
- **点「开始」审的一定是卡片上显示的那个 PR。** 输入 `#123` 这种裸引用时,显示用的是按本地仓库路径解析出的 owner/repo,但提交上去的是原始文本,后端又按路径解析了第二次 —— 改路径不改查询串,于是旧的预览还留在屏幕上、开始按钮也还开着,在防抖窗口里点下去审的是另一个仓库的 #123,而卡片显示的还是第一个。现在直接用 GitHub 面板解析出来的 `owner/repo#n` 发起评审,显示的与实际审的不可能再分叉。(#67)

### 升级须知

- 数据库结构升级到 v21(0.6.0 是 v20),旧库首次启动自动迁移,无需操作。**迁移是单向的** —— 装回 0.6.0 仍能打开迁移过的库,只是新增列上的内容(评审选定的 diff 基线)它看不见,也不会写回去。真要降级,先备份 `~/Library/Application Support/Duetlens/duetlens.db`。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.6.0] - 2026-08-20

### 新增

- **对抗强度的自检轮改为以工具证据为准。** 之前的自检是在同一个线程里让 agent 重新掂量自己的结论 —— 第一轮的推理链还留在上下文里,搜索空间早被剪过一遍,收益有限;而 agent 评审真正的失效方式不是漏报,是凭空捏一条并不存在的调用路径,噪声比遗漏更伤,它会教人整体忽略这个渠道。现在自检有了 `judge_finding`:裁决只有「成立 / 不成立 / 无法验证」三种,必须写理由,而且只有这一轮真的用 `get_file` 或 `search_code` 读过该 finding 所在文件才会被接受 —— 正文里的引用可以编,后端记下的工具调用编不了。「无法验证」刻意与「成立」分开:没有证据不等于已证实。自检先裁决已上报的 finding,再去找漏掉的,没有可裁决的就整轮跳过。同时新增 `search_code`(字面量 `git grep`),让 agent 能自己核对某个符号或调用点是否真的存在;它只在源里有真实代码树时声明,github-pr 评审不会看见一个用不了的工具。裁决只是标注:不动严重级,也不动去留 —— 降级一条 finding 等于软性剔除,而剔除是评审者的事。(#52)
- **后台下载好的更新会在左侧 rail 上标出来。** 此前更新下载完只有设置屏看得见,界面上没有任何东西告诉你「重启一下就升级了」。现在更新处于「已就绪」时设置按钮带一颗未读点,悬浮提示写明新版本在等重启;从这颗按钮进设置会直接滚到「关于」一节,重启按钮就在视野里。只有「已就绪」会亮 —— 下载中和出错时没有可做的事。(#56)

### 修复

- **仓库里指向外部的符号链接不再被读出来。** 路径检查只做了字面比较,而读文件是跟随符号链接的:一个内容为 `leak -> ~/.ssh/id_rsa` 的链接会以 `<仓库>/leak` 通过检查,然后把私钥原样读出来交给 agent 的取证工具。现在链接目标先解析成真实路径再与仓库根重新比对,留在仓库内部的链接照常可用。(#54)
- **对抗自检的证据判定不再冤枉 agent,代价说明也改了。** 证据键此前不折叠 `.` / `..`,而 `a/x/../b.ts` 实际读到的就是 `a/b.ts`,于是明明读过却被拒绝裁决;它还会重写反斜杠、裁掉首尾空格,可这两者都是合法的文件名字符,归并之后 agent 能读一个文件却给另一个下结论 —— 前者只是让人多试一次,后者是静默放行。强度提示里「多花三分之一」的说法出自单次实测,而那次恰好是四次里最便宜的一次,现已按实际区间重写。另外 `category` 很长时严重级徽标会溢出,现在会截断并保留完整文本的悬浮提示。(#53)
- **设置屏状态行前的那颗圆点不再是一块空白。** 圆点的底色只写在语气类(正常 / 警告 / 错误 / 检查中)上,而更新行的「已是最新」与「空闲」不属于任何一类,于是圆点透明却仍占着 7px 宽和 6px 间距,看起来像图标丢了、文字左边空出一块。(#51)
- **扫描条上的覆盖计数改说「已读」。** 原文是「N/M 改动文件已取证」,取证是个法庭用词,这个数看起来像 agent 在给代码立案,而它只表示 agent 真的读过这些文件。空列表表头与悬浮提示一并改。(#55)

### 升级须知

- 数据库结构升级到 v20(0.5.0 是 v18),旧库首次启动自动迁移,无需操作。**迁移是单向的** —— 装回 0.5.0 仍能打开迁移过的库,只是新增列上的内容(finding 由哪一类 turn 报出、自检的裁决与轮次)它看不见,也不会写回去。真要降级,先备份 `~/Library/Application Support/Duetlens/duetlens.db`。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.5.0] - 2026-08-18

### 新增

- **入口页的「最近的审核」只列最近 20 条。** 记录攒多了之后这一列会一直往下长,而入口页是「挑一件事开始做」的地方,再往前的记录本就该去左侧 rail 的审核历史屏翻 —— 那里有搜索、筛选与分组。标题旁的计数改读实际列出的条数,不会再出现「写着 35 却只列 20 行」。(#49)

### 修复

- **分支选择菜单里用鼠标点一下,能选中那个分支了。** 菜单行是不可聚焦的 div,按下鼠标时焦点从过滤输入框上掉走,blur 处理器看到 `relatedTarget` 为空就判定「离开了组件」并关掉菜单,行在 mouseup 之前就已卸载 —— click 事件从头到尾没发生过。表现是除了自动选中的那个默认分支,其余哪一行都点不动,只有方向键加 Enter 走得通。0.4.0 的 #43 起就是这样。(#48)

### 升级须知

- 数据库结构不变,仍是 v18,与 0.4.0 之间可以来回安装。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.4.0] - 2026-08-13

### 新增

- **扫描进行时能看见 agent 在做什么了。** 之前阶段步进器会停在「读取 diff」上好几分钟,画面上唯一动的东西是一颗转圈 —— 而实测 203 次真实扫描,一轮中位数 181 秒、p90 460 秒,第一条 finding 中位数要 169 秒才出现,所以开头那几分钟里 finding 计数一直是 0。现在扫描条上多一行实时动作(在读哪个文件、在搜什么、跑了多久),展开面板与空列表里都有一份动作流,另外给出「已取证 N / M 个改动文件」的覆盖计数。刻意不做百分比进度条:agent 探索多久事先无从声明,分母是编的;覆盖计数也只是覆盖,不代表完成度。(#46)
- **提交屏与导出屏上直接能发起重跑,并配了快捷键 `⌘E`(非 macOS 为 `Ctrl+E`)。** 把评审提交到 GitHub 或导出报告之后,下一步通常就是等作者改完再跑一轮,而这一步此前只能退回 diff 屏去顶栏找按钮。github-pr 评审的入口(按钮与快捷键都是)只在提交成功后出现 —— 离开提交屏会连同没写完的 Review 意见一起卸载掉,不该给一条会丢草稿的路径。(#40)
- **切换本地仓库时可以直接从最近评审过的仓库里挑。** 头部那颗切换按钮现在展开一个菜单,当前仓库置顶并打勾,选目录的系统对话框留在最后一项,不用每次都走一遍文件对话框。没有其他候选时仍是原来的普通按钮。(#42)
- **有官网了**:<https://xieziyu.github.io/duetlens/>,两份 README 顶部都有链接。

### 修复

- **拖动侧栏分隔线不再跟不上光标。** 拖动全程没有节流,每个 `pointermove` 都在改三栏网格的列宽,而中间那栏是完整渲染、没有虚拟化的 diff —— 每个事件都要付一次整棵子树的重排,拖久了分隔线就和指针分家。现在拖动只移动一条虚线幽灵线(合成器路径,代价与 diff 大小无关),松手时才写一次真实列宽,旁边跟着显示目标像素值。顺带修掉两处:窗口在窄断点下有列宽上限,而拖动此前不认这个上限,预览与提交的值都会比实际布局大;以及在窗口外松开鼠标会让幽灵线永久留在屏幕上。(#39)
- **macOS 上的 Ctrl 行内编辑键不再被评审快捷键吞掉。** 评审屏的导航监听和 diff 查找监听都判的是 `metaKey || ctrlKey`,而 macOS 的 Ctrl+F/B/A/E/K 是文本控件的行内编辑键 —— 在讨论区输入框或 finding 编辑器里按 Ctrl+F,焦点会被抢到查找栏去。现在这些快捷键在 macOS 上只认 `⌘`。(#41)
- **分支选择菜单会正常关闭,方向键选中的分支对读屏可见。** Tab 把焦点移走之后菜单仍开着,盖住下方控件;Escape 与选中后焦点会交还给触发按钮,而不是掉到 body 上;长分支列表里选中行会自动滚进视野。(#43)

### 升级须知

- 数据库结构不变,仍是 v18,与 0.3.0 之间可以来回安装。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.3.0] - 2026-08-08

### 新增

- **左栏文件列表多了一个目录树视图。** 与原来的平铺视图一键切换,逐层展开折叠;只有一个子目录的链会压成一行(`src/backend/store`),折叠着的目录行带子树文件数与 finding 徽标。过滤时匹配到的路径自动展开,但不动你手动折叠的状态,清掉过滤词就回到原样。视图偏好全局保存(设置屏里也能改),折叠状态按评审保留、不落库。(#37)
- **明暗模式多了「跟随系统」。** rail 底部那颗钮现在在 浅色 → 深色 → 跟随系统 之间循环,跟随系统时随 OS 实时切换,不用重开应用。顺带修掉一个一直都在的问题:渲染层要等设置读回来才知道该用哪套,所以不用深色的人每次启动都会先看见一帧深色。(#36)
- **重跑启动时能看见进度了。** 之前面板停在一颗禁用的「启动中…」上,后台其实在拆旧会话、拉最新 diff、读 PR 评论 —— 大 PR 上要几十秒,期间界面毫无动静。现在面板原地变成等待视图,四个阶段与入口屏一致,每阶段显示耗时,超过 6 秒补一句「比预期慢」。失败仍退回表单,备注与强度都还在。(#34)

### 修复

- **输入法选词时的 Enter / Esc 不再被当成真实按键。** 确认候选词的 Enter 和取消候选的 Esc 与手敲的那一下无从区分,8 处处理器全都照单全收:剔除理由输入框会存下半句话,提问框与编辑器的 Esc 会把整段草稿丢掉。(#25)
- **正文被改写后,过期的复核说明不再顶替它发给作者。** 「仍存在」的复核说明写在正文之前,正文一旦改写,卡片上就并排出现同一个问题的两种描述,而发给作者的恰恰是旧那份;同轮没刷新的 suggestion 同理,作者一键采纳会覆盖掉他刚改过的代码。现在改写会把两者一起清掉。同时,只改写了正文、没有新复核说明的 finding 不再掉出提交队列。(#35)
- **suggestion 的补丁按锚定行补齐缩进。** agent 给出的补丁常把行首缩进吃掉,而这段是原样提交到 GitHub 的 —— 作者点「Commit suggestion」就把那一行挪到了第 0 列,在 TS 里只是难看,在 Python / YAML 里是直接改坏语义。现在读取时按锚定行对齐,多行补丁整体平移;提交与导出改为对着最新的 head diff 对齐并锁定 commit id,不再与评审时的快照错位。(#31)
- **提交到 PR 正文里的那一节改名为「其他意见(未落在改动行上)」。** 这节装的是没能作为行内评论发出去的 finding,其中一部分带着明确的 `file:line`,叫它「整体意见」与事实矛盾 —— 真正的整体意见是紧挨在它上面的评审者原话。(#26)

### 升级须知

- 数据库结构升级到 v18(0.2.x 是 v16),旧库首次启动自动迁移,无需操作。**迁移是单向的** —— 装回 0.2.x 仍能打开迁移过的库,只是新增列上的内容(正文轮次、文件列表视图偏好)它看不见,也不会写回去。真要降级,先备份 `~/Library/Application Support/Duetlens/duetlens.db`。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.2.2] - 2026-07-31

### 修复

- **GitButler 仓库不再被误判成「不是 GitButler 项目」。** `but` 在 0.22 把 JSON 输出开关从 `--format json` 改名成 `--json`,而三处调用还在传旧写法:入口屏因此报「该目录不是 GitButler 项目(未 setup)」并退回纯 git 评审,分支卡片的改动文件数显示为 0,评审真跑起来时取 diff 直接失败。现在会自动挑本机 `but` 认的那种写法,新旧 CLI 都能用。(#22)
- **github 配色现在真的是 GitHub 的样子。** 之前它只管代码那半边,应用外壳仍是 duetlens 蓝;语法配色本身也停在几个已废弃的旧值上,并且按 duetlens 的语法口味分组 —— 而 GitHub 把字面量算作常量、标签单独占一档、属性不算类型、橙色只留给类型与类名。这版语法色按 Primer 现行 tokens 重做,外壳一并换成 Primer 表面色、6px 圆角与 GitHub 绿主按钮。duetlens 与 parchment 两套配色的渲染结果不变。(#23)

### 升级须知

- 数据库结构不变,仍是 v16,与 0.2.0 / 0.2.1 之间可以来回安装。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.2.1] - 2026-07-30

### 修复

- **「停止机审」按下去真的能停。** 之前它会失败在 `missing field turnId` 上,那一轮继续烧 token 跑到自然结束,而错误卡反过来说这轮「没能开跑」。顺带修掉同一条路上的另一处:停止不再打断排在后面的追问轮。(#19)
- **本机 codex 与这版协议对不上时拒绝开工,而不是在未知策略下静默跑完。** 起会话时我们注入只读沙箱与「不询问审批」,但 codex 对不认识的字段是静默忽略的 —— 字段一旦改名,请求照样「发送成功」,评审 agent 却已经不是只读,且没有任何地方会报出来。现在启动时读回生效策略核对,对不上就不开;会话中出现任何策略审批请求都视为失败证据,终止该轮并拆掉会话。这两类失败各有自己的说明,不再是一行裸的 `-32600` 配一个「重试」——这种失败重试一次结果一样。(#20)

### 升级须知

- 数据库结构不变,仍是 v16,与 0.2.0 之间可以来回安装。
- 这版对齐的 codex 版本是 0.145.0。若启动评审时提示协议对不上,升级本机 codex 即可 —— 变化只是把原先的静默降级换成了明确拒绝。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.2.0] - 2026-07-29

### 新增

- **讨论区的结论可以一键写回 finding。** 追问轮里 agent 不再直接改动,而是给出一张确认卡片,由你点了才生效;扫描轮与自查轮仍直接落库。同时补上「剔除」这个动作 —— 在此之前 agent 没有办法表达「这条不成立」,只能把理由写进正文,把原文覆盖掉。(#12)
- **扫描结束时 agent 会写一份只读的评审摘要**,附上「值得人工再看一眼」的文件清单,可点击跳转。摘要是给评审者看的材料,不随提交发给 PR 作者,也不进导出报告 —— 机器写的句子要发出去,得由人再写一遍。重跑后若新一轮没写摘要,旧摘要会标出来自哪一轮。(#14)
- **github-pr 评审也能导出 Markdown 报告。** 提交与导出并列在同一屏,共用一套 finding 与三态分档;报告里每条标注是否已提交,可切换「全部保留项 / 仅未提交」,文件名带 PR 号。(#17)
- **降级到「整体意见」的 finding 保留文件锚点**:摘要条目带 `file:line`,并且可以退回行内评论。(#13)
- **已剔除的 finding 收进独立分组**,默认折叠,不再混在原严重度分组里占位。(#15)
- **新增 parchment(羊皮纸)配色**,明暗各一套。(#10)
- **提示词规则屏**:去掉右侧合并预览列,编辑区铺满;未选仓库时也能只读浏览全部小节,项目层显示当前仓库并可切换。(#11)

### 修复

- **重跑时,同一位置上的新问题不再被当成已修复的旧 finding。** 旧逻辑会把已关闭的那条恢复成待处理并吞掉新报告,结果是卡片显示「已修复」而列表仍在待办里,且新问题无声丢失。(#16)
- **提交的 suggestion 保留首行缩进** —— 之前一键采纳会把锚定行悄悄改变缩进。(#8)
- **提交进行中重复点击不再发出两份评论**:中途离开提交屏再回来会重置本地状态,那第二次点击在界面上完全合法。(#17)
- **入口屏最近仓库名不再折行**,改为路径先截断。(#9)

### 升级须知

- 数据库结构升级到 v16,旧库首次启动自动迁移,无需操作。**迁移是单向的** —— 装回 0.1.0 仍能打开这个库,但新增列上的内容(摘要、锚点降级标记等)它一概看不见,也不会写回去。真要降级,先备份 `~/Library/Application Support/Duetlens/duetlens.db`。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.1.0] - 2026-07-26

首个公开版本。

[0.9.0]: https://github.com/xieziyu/duetlens/compare/v0.8.0...v0.9.0
[0.8.0]: https://github.com/xieziyu/duetlens/compare/v0.7.2...v0.8.0
[0.7.2]: https://github.com/xieziyu/duetlens/compare/v0.7.1...v0.7.2
[0.7.1]: https://github.com/xieziyu/duetlens/compare/v0.7.0...v0.7.1
[0.7.0]: https://github.com/xieziyu/duetlens/compare/v0.6.0...v0.7.0
[0.6.0]: https://github.com/xieziyu/duetlens/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/xieziyu/duetlens/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/xieziyu/duetlens/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/xieziyu/duetlens/compare/v0.2.2...v0.3.0
[0.2.2]: https://github.com/xieziyu/duetlens/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/xieziyu/duetlens/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/xieziyu/duetlens/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/xieziyu/duetlens/releases/tag/v0.1.0
