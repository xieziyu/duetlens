# 更新日志

本文件只记**对使用者可见**的变化。内部重构、文档与 CI 调整不单列,查 `git log`。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/);`0.x` 阶段 minor 位即破坏性变更位。

## [0.4.0] - 2026-08-13

### 新增

- **扫描进行时能看见 agent 在做什么了。** 之前阶段步进器会停在「读取 diff」上好几分钟,画面上唯一动的东西是一颗转圈 —— 而实测 203 次真实扫描,一轮中位数 181 秒、p90 460 秒,第一条 finding 中位数要 169 秒才出现,所以开头那几分钟里 finding 计数一直是 0。现在扫描条上多一行实时动作(在读哪个文件、在搜什么、跑了多久),展开面板与空列表里都有一份动作流,另外给出「已取证 N / M 个改动文件」的覆盖计数。刻意不做百分比进度条:agent 探索多久事先无从声明,分母是编的;覆盖计数也只是覆盖,不代表完成度。(#46)
- **提交屏与导出屏上直接能发起重跑,并配了快捷键 `⌘E`(非 macOS 为 `Ctrl+E`)。** 把评审提交到 GitHub 或导出报告之后,下一步通常就是等作者改完再跑一轮,而这一步此前只能退回 diff 屏去顶栏找按钮。github-pr 评审的入口(按钮与快捷键都是)只在提交成功后出现 —— 离开提交屏会连同没写完的 Review 意见一起卸载掉,不该给一条会丢草稿的路径。(#40)
- **切换本地仓库时可以直接从最近评审过的仓库里挑。** 头部那颗切换按钮现在展开一个菜单,当前仓库置顶并打勾,选目录的系统对话框留在最后一项,不用每次都走一遍文件对话框。没有其他候选时仍是原来的普通按钮。(#42)
- **有官网了**:<https://xieziyu.github.io/duetlens/>,两份 README 顶部都有链接。

### 修复

- **拖动侧栏分隔线不再跟不上光标。** 拖动全程没有节流,每个 `pointermove` 都在改三栏网格的列宽,而中间那栏是完整渲染、没有虚拟化的 diff —— 每个事件都要付一次整棵子树的重排,拖久了分隔线就和指针分家。现在拖动只移动一条虚线幽灵线(合成器路径,代价与 diff 大小无关),松手时才写一次真实列宽,旁边跟着显示目标像素值。顺带修掉两处:窗口在窄断点下有列宽上限,而拖动此前不认这个上限,预览与提交的值都会比实际布局大;以及在窗口外松开鼠标会让幽灵线永久留在屏幕上。(#39)
- **macOS 上的 Ctrl 行内编辑键不再被评审快捷键吞掉。** 评审屏的导航监听和 diff 查找监听都判的是 `metaKey || ctrlKey`,而 macOS 的 Ctrl+F/B/A/E/K 是文本控件的行内编辑键 —— 在讨论区输入框或 finding 编辑器里按 Ctrl+F,焦点会被抢到查找栏去。现在这些快捷键在 macOS 上只认 `⌘`。(#41)
- **分支选择菜单会正常关闭,方向键选中的分支对读屏可见。** Tab 把焦点移走之后菜单仍开着,盖住下方控件;Escape 与选中后焦点会交还给触发按钮,而不是掉到 body 上;长分支列表里选中行会自动滚进视野。(#43)

### 升级须知

- 数据库结构不变,仍是 v18,与 0.3.0 之间可以来回安装。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.3.0] - 2026-08-08

### 新增

- **左栏文件列表多了一个目录树视图。** 与原来的平铺视图一键切换,逐层展开折叠;只有一个子目录的链会压成一行(`src/backend/store`),折叠着的目录行带子树文件数与 finding 徽标。过滤时匹配到的路径自动展开,但不动你手动折叠的状态,清掉过滤词就回到原样。视图偏好全局保存(设置屏里也能改),折叠状态按评审保留、不落库。(#37)
- **明暗模式多了「跟随系统」。** rail 底部那颗钮现在在 浅色 → 深色 → 跟随系统 之间循环,跟随系统时随 OS 实时切换,不用重开应用。顺带修掉一个一直都在的问题:渲染层要等设置读回来才知道该用哪套,所以不用深色的人每次启动都会先看见一帧深色。(#36)
- **重跑启动时能看见进度了。** 之前面板停在一颗禁用的「启动中…」上,后台其实在拆旧会话、拉最新 diff、读 PR 评论 —— 大 PR 上要几十秒,期间界面毫无动静。现在面板原地变成等待视图,四个阶段与入口屏一致,每阶段显示耗时,超过 6 秒补一句「比预期慢」。失败仍退回表单,备注与强度都还在。(#34)

### 修复

- **输入法选词时的 Enter / Esc 不再被当成真实按键。** 确认候选词的 Enter 和取消候选的 Esc 与手敲的那一下无从区分,8 处处理器全都照单全收:剔除理由输入框会存下半句话,提问框与编辑器的 Esc 会把整段草稿丢掉。(#25)
- **正文被改写后,过期的复核说明不再顶替它发给作者。** 「仍存在」的复核说明写在正文之前,正文一旦改写,卡片上就并排出现同一个问题的两种描述,而发给作者的恰恰是旧那份;同轮没刷新的 suggestion 同理,作者一键采纳会覆盖掉他刚改过的代码。现在改写会把两者一起清掉。同时,只改写了正文、没有新复核说明的 finding 不再掉出提交队列。(#35)
- **suggestion 的补丁按锚定行补齐缩进。** agent 给出的补丁常把行首缩进吃掉,而这段是原样提交到 GitHub 的 —— 作者点「Commit suggestion」就把那一行挪到了第 0 列,在 TS 里只是难看,在 Python / YAML 里是直接改坏语义。现在读取时按锚定行对齐,多行补丁整体平移;提交与导出改为对着最新的 head diff 对齐并锁定 commit id,不再与评审时的快照错位。(#31)
- **提交到 PR 正文里的那一节改名为「其他意见(未落在改动行上)」。** 这节装的是没能作为行内评论发出去的 finding,其中一部分带着明确的 `file:line`,叫它「整体意见」与事实矛盾 —— 真正的整体意见是紧挨在它上面的评审者原话。(#26)

### 升级须知

- 数据库结构升级到 v18(0.2.x 是 v16),旧库首次启动自动迁移,无需操作。**迁移是单向的** —— 装回 0.2.x 仍能打开迁移过的库,只是新增列上的内容(正文轮次、文件列表视图偏好)它看不见,也不会写回去。真要降级,先备份 `~/Library/Application Support/Duetlens/duetlens.db`。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.2.2] - 2026-07-31

### 修复

- **GitButler 仓库不再被误判成「不是 GitButler 项目」。** `but` 在 0.22 把 JSON 输出开关从 `--format json` 改名成 `--json`,而三处调用还在传旧写法:入口屏因此报「该目录不是 GitButler 项目(未 setup)」并退回纯 git 评审,分支卡片的改动文件数显示为 0,评审真跑起来时取 diff 直接失败。现在会自动挑本机 `but` 认的那种写法,新旧 CLI 都能用。(#22)
- **github 配色现在真的是 GitHub 的样子。** 之前它只管代码那半边,应用外壳仍是 duetlens 蓝;语法配色本身也停在几个已废弃的旧值上,并且按 duetlens 的语法口味分组 —— 而 GitHub 把字面量算作常量、标签单独占一档、属性不算类型、橙色只留给类型与类名。这版语法色按 Primer 现行 tokens 重做,外壳一并换成 Primer 表面色、6px 圆角与 GitHub 绿主按钮。duetlens 与 parchment 两套配色的渲染结果不变。(#23)

### 升级须知

- 数据库结构不变,仍是 v16,与 0.2.0 / 0.2.1 之间可以来回安装。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.2.1] - 2026-07-30

### 修复

- **「停止机审」按下去真的能停。** 之前它会失败在 `missing field turnId` 上,那一轮继续烧 token 跑到自然结束,而错误卡反过来说这轮「没能开跑」。顺带修掉同一条路上的另一处:停止不再打断排在后面的追问轮。(#19)
- **本机 codex 与这版协议对不上时拒绝开工,而不是在未知策略下静默跑完。** 起会话时我们注入只读沙箱与「不询问审批」,但 codex 对不认识的字段是静默忽略的 —— 字段一旦改名,请求照样「发送成功」,评审 agent 却已经不是只读,且没有任何地方会报出来。现在启动时读回生效策略核对,对不上就不开;会话中出现任何策略审批请求都视为失败证据,终止该轮并拆掉会话。这两类失败各有自己的说明,不再是一行裸的 `-32600` 配一个「重试」——这种失败重试一次结果一样。(#20)

### 升级须知

- 数据库结构不变,仍是 v16,与 0.2.0 之间可以来回安装。
- 这版对齐的 codex 版本是 0.145.0。若启动评审时提示协议对不上,升级本机 codex 即可 —— 变化只是把原先的静默降级换成了明确拒绝。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.2.0] - 2026-07-29

### 新增

- **讨论区的结论可以一键写回 finding。** 追问轮里 agent 不再直接改动,而是给出一张确认卡片,由你点了才生效;扫描轮与自查轮仍直接落库。同时补上「剔除」这个动作 —— 在此之前 agent 没有办法表达「这条不成立」,只能把理由写进正文,把原文覆盖掉。(#12)
- **扫描结束时 agent 会写一份只读的评审摘要**,附上「值得人工再看一眼」的文件清单,可点击跳转。摘要是给评审者看的材料,不随提交发给 PR 作者,也不进导出报告 —— 机器写的句子要发出去,得由人再写一遍。重跑后若新一轮没写摘要,旧摘要会标出来自哪一轮。(#14)
- **github-pr 评审也能导出 Markdown 报告。** 提交与导出并列在同一屏,共用一套 finding 与三态分档;报告里每条标注是否已提交,可切换「全部保留项 / 仅未提交」,文件名带 PR 号。(#17)
- **降级到「整体意见」的 finding 保留文件锚点**:摘要条目带 `file:line`,并且可以退回行内评论。(#13)
- **已剔除的 finding 收进独立分组**,默认折叠,不再混在原严重度分组里占位。(#15)
- **新增 parchment(羊皮纸)配色**,明暗各一套。(#10)
- **提示词规则屏**:去掉右侧合并预览列,编辑区铺满;未选仓库时也能只读浏览全部小节,项目层显示当前仓库并可切换。(#11)

### 修复

- **重跑时,同一位置上的新问题不再被当成已修复的旧 finding。** 旧逻辑会把已关闭的那条恢复成待处理并吞掉新报告,结果是卡片显示「已修复」而列表仍在待办里,且新问题无声丢失。(#16)
- **提交的 suggestion 保留首行缩进** —— 之前一键采纳会把锚定行悄悄改变缩进。(#8)
- **提交进行中重复点击不再发出两份评论**:中途离开提交屏再回来会重置本地状态,那第二次点击在界面上完全合法。(#17)
- **入口屏最近仓库名不再折行**,改为路径先截断。(#9)

### 升级须知

- 数据库结构升级到 v16,旧库首次启动自动迁移,无需操作。**迁移是单向的** —— 装回 0.1.0 仍能打开这个库,但新增列上的内容(摘要、锚点降级标记等)它一概看不见,也不会写回去。真要降级,先备份 `~/Library/Application Support/Duetlens/duetlens.db`。
- 更新由应用内 updater 接手,无需重新下载 dmg。

## [0.1.0] - 2026-07-26

首个公开版本。

[0.4.0]: https://github.com/xieziyu/duetlens/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/xieziyu/duetlens/compare/v0.2.2...v0.3.0
[0.2.2]: https://github.com/xieziyu/duetlens/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/xieziyu/duetlens/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/xieziyu/duetlens/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/xieziyu/duetlens/releases/tag/v0.1.0
